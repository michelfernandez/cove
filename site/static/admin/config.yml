backend:
  name: git-gateway
  branch: master
media_folder: "static/images/uploads"
public_folder: "/"
collections:
  - name: home
    label: "Home"
    folder: "/"
    fields:
      - name: title
        widget: "string"
      - name: "body"
        widget: "markdown"
